import 'package:flutter/material.dart';

class MonthlyBreakdown extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Breakdown', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}

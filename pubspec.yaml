name: finalproject
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  image_picker: ^1.0.7
  http: ^1.2.0
  cupertino_icons: ^1.0.8
  fl_chart: ^1.1.1

  # Database (for Android/iOS)
  sqflite: ^2.3.3+1
  path_provider: ^2.1.5

  # Existing project packages (used by your teammates)
  flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # You can keep this if you want to ship a prefilled db later.
  assets:
    - assets/databases/walletflow.db
